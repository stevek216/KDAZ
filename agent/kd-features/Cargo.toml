[package]
name = "kingdomino-features"
version = "0.0.1"
edition = "2021"
description = "Shared agent-side feature encoding + observation JSON for Kingdomino"
license = "MIT"

[dependencies]
serde_json = "1"
kingdomino-engine = { path = "../../board-game-engine" }
