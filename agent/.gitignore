# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# maturin / Rust build output
target/
*.pyd
*.so

# AlphaZero artifacts
checkpoints/
models/
*.pt
*.pth
*.safetensors
logs/
# self-play corpora — large, never commit
data/
replay_buffer/
traces/

# ...but DO version trained checkpoints so the repo is clone-and-go.
# Everything else in runs/ (traced models *.ts.pt, parity dumps) stays ignored.
runs/*
!runs/*.best.pt
!runs/*.last.pt
